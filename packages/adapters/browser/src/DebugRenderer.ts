import { EngineSnapshot } from '@tabverse/engine';

export class DebugRenderer {
  // canvas and ctx removed as they are unused in stub

  constructor(_canvas: HTMLCanvasElement) {
  }

  public render(_snapshot: EngineSnapshot) {
    // Render topology overlays, normals, bounding boxes
  }
}
